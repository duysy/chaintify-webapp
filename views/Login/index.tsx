import React from "react";
import { Box, Stack, Button, Typography } from "@mui/material";
import Wrap from "../wrap";
export default function LoginView() {
  return (
    <Wrap>
      <Stack>
        <Typography>Connect wallet now</Typography>
      </Stack>
    </Wrap>
  );
}
